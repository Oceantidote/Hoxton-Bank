<div class="card-section">
  <h4 class="dash-header">Transaction Failed</h4>
</div>
<div class="choose-section">
  <div class="forgot-button">
    <i class="fas fa-times-circle red bigicon"></i>
  </div>
  <% transaction["failure_reasons"].each do |reason| %>
    <h6 class="tc"><%= ApplicationRecord.convert_reason(reason) %></h6>
  <% end %>
  <h6 class="tc">Remaining Balance:</h6>
  <h6 class="tc"><span class="numbers">£ <%= ApplicationRecord.to_money(@ledger["amount"]) %></span></h6>
  <%= link_to send_money_path do %>
    <div class="btn railsbank-btn">Make another payment</div>
  <% end %>
</div>
